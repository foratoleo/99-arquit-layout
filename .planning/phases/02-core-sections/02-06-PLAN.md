---
phase: 02-core-sections
plan: 06
type: execute
wave: 3
depends_on: ["02-01"]
files_modified: [components/Materiality.tsx, app/page.tsx]
autonomous: false
user_setup: []

must_haves:
  truths:
    - "Materiality section showcases joinery and stonework"
    - "Placeholder images are present for macro photography"
    - "Fade-in animation triggers when section enters viewport"
    - "Section layout provides space for material craftsmanship showcase"
    - "Content describes the importance of material quality"
  artifacts:
    - path: "components/Materiality.tsx"
      provides: "Materiality section with placeholder macro photography"
      min_lines: 50
    - path: "app/page.tsx"
      provides: "Home page with Materiality section"
      contains: "Materiality"
  key_links:
    - from: "components/Materiality.tsx"
      to: "components/SectionWrapper.tsx"
      via: "import for fade-in"
      pattern: "from.*SectionWrapper"
    - from: "app/page.tsx"
      to: "components/Materiality"
      via: "import"
      pattern: "import.*Materiality"
---

<objective>
Create the Materiality section showcasing joinery and stonework craftsmanship with placeholder macro photography.

Purpose: Per 02-CONTEXT.md and research, this section demonstrates the firm's focus on material excellence. Placeholder images will be replaced with actual macro photography of joinery and stonework in Phase 4 (final content).

Output: Working Materiality section with placeholder images and descriptive content about material craftsmanship.
</objective>

<execution_context>
@/Users/forato/.claude/get-shit-done/workflows/execute-plan.md
@/Users/forato/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-core-sections/02-CONTEXT.md

# User Decisions (LOCKED from 02-CONTEXT.md)
- Materiality section placeholder will be replaced with macro photography later
- Focus on joinery and stonework craftsmanship

# Requirements
- **CONT-04**: Materiality section showcasing joinery and stonework with macro photography

# Prior Work
Plan 02-01: Next.js project initialized with SectionWrapper component
Plan 02-02: Hero section created
Plan 02-04: Manifesto section created
Plan 02-05: Technical Differentiator section created
</context>

<tasks>

<task type="auto">
  <name>Create Materiality section component with placeholder images</name>
  <files>components/Materiality.tsx, public/materiality-1.jpg, public/materiality-2.jpg, public/materiality-3.jpg</files>
  <action>
Create `components/Materiality.tsx` using SectionWrapper and placeholder macro photography:

```typescript
import SectionWrapper from './SectionWrapper';
import Image from 'next/image';

export default function Materiality() {
  const materials = [
    {
      id: 1,
      title: 'Marcenaria Artesanal',
      description: 'Cada junta é precisa. Cada encaixe, perfeito. A madeira conta histórias através do trabalho manual que transforma matéria em arte.',
      image: '/materiality-1.jpg',
    },
    {
      id: 2,
      title: 'Pedra Natural',
      description: 'A pedra bruta dá lugar a superfícies que convidam ao toque. Veios que desenham paisagens, texturas que guardam a memória da terra.',
      image: '/materiality-2.jpg',
    },
    {
      id: 3,
      title: 'Detalhe que Diferencia',
      description: 'Na sutileza do acabamento está a excelência. O que parece simples é o resultado de décadas de domínio técnico e sensibilidade estética.',
      image: '/materiality-3.jpg',
    },
  ];

  return (
    <SectionWrapper
      id="materialidade"
      className="relative bg-sand-50 px-6 py-32"
    >
      <div className="mx-auto max-w-7xl">
        {/* Section header */}
        <div className="mb-20 text-center">
          <h2 className="mb-6 font-serif text-4xl text-gray-900 md:text-5xl lg:text-6xl">
            Materialidade
          </h2>
          <p className="mx-auto max-w-2xl font-serif text-lg text-gray-600 md:text-xl">
            A excelência está nos detalhes que poucos veem, mas todos sentem.
          </p>
          <div className="mx-auto mt-8 h-0.5 w-24 bg-greige-400" />
        </div>

        {/* Material showcase grid */}
        <div className="grid grid-cols-1 gap-12 md:grid-cols-2 lg:grid-cols-3">
          {materials.map((material, index) => (
            <div
              key={material.id}
              className="group flex flex-col overflow-hidden bg-white"
            >
              {/* Image placeholder */}
              <div className="relative aspect-[4/5] overflow-hidden bg-sand-100">
                <Image
                  src={material.image}
                  alt={material.title}
                  fill
                  className="object-cover transition-transform duration-700 group-hover:scale-105"
                  sizes="(max-width: 768px) 100vw, (max-width: 1024px) 50vw, 33vw"
                  quality={85}
                />
                {/* Placeholder overlay when image doesn't exist */}
                <div className="absolute inset-0 flex items-center justify-center bg-greige-100">
                  <div className="text-center">
                    <p className="font-serif text-xl text-greige-400">
                      Macro Fotografia
                    </p>
                    <p className="mt-2 font-serif text-sm text-greige-500">
                      {material.title}
                    </p>
                  </div>
                </div>
              </div>

              {/* Content */}
              <div className="flex flex-grow flex-col justify-center p-8">
                <h3 className="mb-4 font-serif text-2xl text-gray-900">
                  {material.title}
                </h3>
                <p className="font-serif text-base leading-relaxed text-gray-600">
                  {material.description}
                </p>
              </div>
            </div>
          ))}
        </div>

        {/* Call to action */}
        <div className="mt-20 text-center">
          <p className="font-serif text-lg text-greige-600">
            Cada material é escolhido com propósito. Cada detalhe, executado com intenção.
          </p>
        </div>
      </div>
    </SectionWrapper>
  );
}
```

Create placeholder images in public directory:
```bash
# Create placeholder files or use placeholder service
# These will be replaced with actual macro photography in Phase 4
touch public/materiality-1.jpg public/materiality-2.jpg public/materiality-3.jpg
```

Key design decisions:
- Grid layout with 3 material showcases (responsive: 1 col mobile, 2 col tablet, 3 col desktop)
- Aspect ratio 4:5 for vertical macro photography
- Hover effect on images (subtle scale)
- Placeholder overlays when actual images aren't available yet
- Descriptive content for joinery, stonework, and craftsmanship
- Section id="materialidade" (PT-BR)
  </action>
  <verify>
Check components/Materiality.tsx exists with proper grid layout and placeholder structure.
  </verify>
  <done>
Materiality component created with placeholder macro photography layout
  </done>
</task>

<task type="auto">
  <name>Integrate Materiality into home page</name>
  <files>app/page.tsx</files>
  <action>
Update `app/page.tsx` to include the Materiality section as the final core section:

```typescript
import Hero from '@/components/Hero';
import Manifesto from '@/components/Manifesto';
import TechnicalDifferentiator from '@/components/TechnicalDifferentiator';
import Materiality from '@/components/Materiality';

export default function Home() {
  return (
    <main className="min-h-screen">
      <Hero />
      <Manifesto />
      <TechnicalDifferentiator />
      <Materiality />
    </main>
  );
}
```

The Materiality now appears as the fourth and final core section. Portfolio and Contact sections will be added in later phases.
  </action>
  <verify>
Run `npm run dev` and verify Materiality section appears as the final section on the home page.
  </verify>
  <done>
Materiality section integrated into home page as final core section
  </done>
</task>

<task type="checkpoint:human-verify">
  <what-built>Materiality section showcasing joinery and stonework with placeholder macro photography, grid layout (3 cards), and fade-in animation when entering viewport</what-built>
  <how-to-verify>
1. Run `npm run dev` and visit http://localhost:3000
2. Scroll down to Materiality section (below Technical Differentiator)
3. Verify the following:
   - Section displays with title "Materialidade" and descriptive subtitle
   - Three material cards are displayed in grid layout
   - Desktop: 3 columns, Tablet: 2 columns, Mobile: 1 column (stacked)
   - Each card has image placeholder and descriptive text
   - Card titles: Marcenaria Artesanal, Pedra Natural, Detalhe que Diferencia
   - Descriptions focus on craftsmanship and material quality
   - Images have subtle hover effect (scale)
   - Fade-in animation triggers when section enters viewport
   - Placeholder overlays show when actual images aren't loaded
   - Section ends with a call to action / statement about materials
   - Overall layout is spacious and luxurious
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues with the Materiality section</resume-signal>
</task>

</tasks>

<verification>
1. Run `npm run dev` - no console errors
2. Scroll to Materiality - fade-in animation triggers
3. Check grid layout is responsive (1/2/3 columns)
4. Verify placeholder images are present
5. Test hover effect on images
6. Check section completes the core content structure
7. Verify page structure: Hero -> Manifesto -> Technical Differentiator -> Materiality
</verification>

<success_criteria>
- Materiality section displays as fourth section below Technical Differentiator
- Three material cards showcase joinery, stonework, and craftsmanship
- Grid layout is responsive (1/2/3 columns based on viewport)
- Placeholder images present (will be replaced with macro photography in Phase 4)
- Descriptive content emphasizes material quality and craftsmanship
- Fade-in animation triggers when section enters viewport
- Hover effect on images (subtle scale)
- Core content structure is complete
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-sections/02-06-SUMMARY.md` with frontmatter documenting Materiality section implementation.

This completes Phase 2: Core Sections. All four main content sections (Hero, Manifesto, Technical Differentiator, Materiality) are now implemented with LCP optimization and smooth animations.
</output>
