---
phase: 02-core-sections
plan: 05
type: execute
wave: 3
depends_on: ["02-01"]
files_modified: [components/TechnicalDifferentiator.tsx, app/page.tsx]
autonomous: false
user_setup: []

must_haves:
  truths:
    - "Technical differentiator section displays split layout (text + visual)"
    - "Content explains vertical construction expertise in accessible language"
    - "Problem-solution format is clear and not overly technical"
    - "Fade-in animation triggers when section enters viewport"
    - "Supporting visual/diagram placeholder is present"
  artifacts:
    - path: "components/TechnicalDifferentiator.tsx"
      provides: "Technical differentiator section with split layout"
      min_lines: 60
    - path: "app/page.tsx"
      provides: "Home page with Technical Differentiator section"
      contains: "TechnicalDifferentiator"
  key_links:
    - from: "components/TechnicalDifferentiator.tsx"
      to: "components/SectionWrapper.tsx"
      via: "import for fade-in"
      pattern: "from.*SectionWrapper"
    - from: "app/page.tsx"
      to: "components/TechnicalDifferentiator"
      via: "import"
      pattern: "import.*TechnicalDifferentiator"
---

<objective>
Create the Technical Differentiator section with split layout (text + visual), problem-solution content format, and accessible overview of vertical construction expertise.

Purpose: Per 02-CONTEXT.md decisions, this section establishes technical authority through a split layout with text on one side and supporting visual (technical diagram/cross-section) on the other. Content uses problem-solution format, written for non-technical audiences.

Output: Working Technical Differentiator section with placeholder technical visual and accessible vertical construction content.
</objective>

<execution_context>
@/Users/forato/.claude/get-shit-done/workflows/execute-plan.md
@/Users/forato/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-core-sections/02-CONTEXT.md

# User Decisions (LOCKED from 02-CONTEXT.md)
- **Visual focus**: Technical visuals — technical diagrams, cross-sections showing expertise
- **Content format**: Problem-solution format — why vertical construction matters, challenges, solutions
- **Layout**: Split layout — text on one side, supporting visual on other
- **Detail level**: Accessible overview — high-level for non-technical audience
- Technical differentiator establishes authority without being overly technical

# Prior Work
Plan 02-01: Next.js project initialized with SectionWrapper component
Plan 02-02: Hero section created
Plan 02-04: Manifesto section created
</context>

<tasks>

<task type="auto">
  <name>Create Technical Differentiator section component</name>
  <files>components/TechnicalDifferentiator.tsx, public/technical-diagram.jpg</files>
  <action>
Create `components/TechnicalDifferentiator.tsx` using SectionWrapper and split layout:

```typescript
import SectionWrapper from './SectionWrapper';
import Image from 'next/image';

export default function TechnicalDifferentiator() {
  return (
    <SectionWrapper
      id="diferencial-tecnico"
      className="relative bg-white px-6 py-32"
    >
      <div className="mx-auto max-w-7xl">
        <div className="grid grid-cols-1 gap-16 lg:grid-cols-2 lg:gap-24">
          {/* Text side - Problem-solution format */}
          <div className="flex flex-col justify-center space-y-12">
            {/* Section title */}
            <div>
              <h2 className="mb-4 font-serif text-4xl text-gray-900 md:text-5xl">
                Verticalização
                <br />
                <span className="text-greige-600">de Excelência</span>
              </h2>
              <div className="h-0.5 w-24 bg-greige-400" />
            </div>

            {/* Problem */}
            <div className="space-y-4">
              <h3 className="font-serif text-xl text-greige-700 md:text-2xl">
                O Desafio
              </h3>
              <p className="font-serif text-lg leading-relaxed text-gray-700">
                Construções verticais demandam precisão milimétrica. Cada laje,
                cada pilar, cada sistema hidráulico e elétrico deve ser
                perfeitamente coordenado. O erro de cálculo em um pavimento
                se multiplica em todos os demais.
              </p>
            </div>

            {/* Solution */}
            <div className="space-y-4">
              <h3 className="font-serif text-xl text-greige-700 md:text-2xl">
                Nossa Solução
              </h3>
              <p className="font-serif text-lg leading-relaxed text-gray-700">
                Integramos arquitetura, engenharia e construção desde o
                primeiro traço. Nossa experiência em projetos de múltiplos
                pavimentos garante a coordenação perfeita entre estrutura,
                instalações e acabamentos — resultando em obras mais eficientes,
                com menos desperdício e qualidade superior.
              </p>
            </div>

            {/* Outcome */}
            <div className="space-y-4">
              <h3 className="font-serif text-xl text-greige-700 md:text-2xl">
                O Resultado
              </h3>
              <p className="font-serif text-lg leading-relaxed text-gray-700">
                Edifícios que respiram funcionalidade em cada detalhe. Espaços
                que funcionam perfeitamente para quem vive e trabalha neles,
                com a durabilidade e a sofisticação que apenas a expertise
                técnica profunda pode proporcionar.
              </p>
            </div>
          </div>

          {/* Visual side - Technical diagram placeholder */}
          <div className="relative flex items-center justify-center">
            <div className="relative aspect-square w-full overflow-hidden rounded-none bg-sand-100 lg:aspect-[4/5]">
              <Image
                src="/technical-diagram.jpg"
                alt="Diagrama técnico de construção vertical"
                fill
                className="object-cover"
                sizes="(max-width: 1024px) 100vw, 50vw"
                quality={85}
              />
              {/* Placeholder overlay if no image */}
              <div className="absolute inset-0 flex items-center justify-center bg-greige-50">
                <div className="text-center">
                  <p className="font-serif text-2xl text-greige-400">
                    Diagrama Técnico
                  </p>
                  <p className="mt-2 font-serif text-sm text-greige-500">
                    (Placeholder para visual técnico)
                  </p>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </SectionWrapper>
  );
}
```

Key design decisions per 02-CONTEXT.md:
- Split layout: text on left, visual on right (grid-cols-2 on lg+)
- Problem-solution format: O Desafio → Nossa Solução → O Resultado
- Accessible language: high-level overview, not overly technical
- Technical visual placeholder: will be replaced with actual diagrams/cross-sections
- Spacious layout with room for content to breathe

Create a placeholder technical diagram or use a simple CSS fallback:
```bash
# For now, the placeholder overlay in the component serves as visual
# Later, add actual technical diagrams to public/technical-diagram.jpg
```

Note: Section id is "diferencial-tecnico" (PT-BR) since the site is PT-BR only per requirements.
  </action>
  <verify>
Check components/TechnicalDifferentiator.tsx exists with proper split layout and problem-solution content structure.
  </verify>
  <done>
Technical Differentiator component created with split layout
  </done>
</task>

<task type="auto">
  <name>Integrate Technical Differentiator into home page</name>
  <files>app/page.tsx</files>
  <action>
Update `app/page.tsx` to include the Technical Differentiator section below Manifesto:

```typescript
import Hero from '@/components/Hero';
import Manifesto from '@/components/Manifesto';
import TechnicalDifferentiator from '@/components/TechnicalDifferentiator';

export default function Home() {
  return (
    <main className="min-h-screen">
      <Hero />
      <Manifesto />
      <TechnicalDifferentiator />
    </main>
  );
}
```

The Technical Differentiator now appears as the third section, below Manifesto.
  </action>
  <verify>
Run `npm run dev` and verify Technical Differentiator section appears below Manifesto on the home page.
  </verify>
  <done>
Technical Differentiator section integrated into home page
  </done>
</task>

<task type="checkpoint:human-verify">
  <what-built>Technical Differentiator section with split layout (text + visual), problem-solution content format (Challenge/Solution/Result), and accessible explanation of vertical construction expertise</what-built>
  <how-to-verify>
1. Run `npm run dev` and visit http://localhost:3000
2. Scroll down to Technical Differentiator section (below Manifesto)
3. Verify the following:
   - Section displays in split layout (text left, visual right on desktop)
   - Text side has clear headings: O Desafio, Nossa Solução, O Resultado
   - Content explains vertical construction in accessible language (not overly technical)
   - Problem-solution format is clear and easy to follow
   - Visual side shows placeholder or technical diagram
   - Fade-in animation triggers when section enters viewport
   - Mobile: layout stacks vertically (text on top, visual below)
   - Typography uses serif font consistent with rest of page
   - Spacing is comfortable and readable
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues with the Technical Differentiator section</resume-signal>
</task>

</tasks>

<verification>
1. Run `npm run dev` - no console errors
2. Scroll to Technical Differentiator - fade-in animation triggers
3. Check layout is split (2 columns on lg+, 1 column on mobile)
4. Verify content follows problem-solution format
5. Test responsive - layout stacks on mobile
6. Check visual placeholder is present
</verification>

<success_criteria>
- Technical Differentiator section displays below Manifesto
- Split layout with text on one side, visual on the other (desktop)
- Problem-solution format (Challenge/Solution/Result) is clear
- Content is accessible (not overly technical) for non-technical audience
- Fade-in animation triggers when section enters viewport
- Mobile responsive layout stacks vertically
- Technical visual placeholder is present (to be replaced with actual diagrams)
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-sections/02-05-SUMMARY.md` with frontmatter documenting Technical Differentiator implementation.
</output>
