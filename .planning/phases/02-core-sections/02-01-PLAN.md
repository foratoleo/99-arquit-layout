---
phase: 02-core-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, next.config.ts, tsconfig.json, tailwind.config.ts, app/layout.tsx, app/globals.css]
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Next.js 15 project initializes without errors"
    - "GSAP, Tailwind CSS, and TypeScript are configured"
    - "Dev server starts successfully with `npm run dev`"
  artifacts:
    - path: "package.json"
      provides: "Dependencies and scripts"
      contains: "next, gsap, tailwindcss, typescript"
    - path: "app/layout.tsx"
      provides: "Root layout with fonts"
      min_lines: 20
    - path: "tailwind.config.ts"
      provides: "Tailwind configuration"
      contains: "content, theme"
    - path: "next.config.ts"
      provides: "Next.js configuration"
      min_lines: 10
  key_links:
    - from: "package.json"
      to: "node_modules"
      via: "npm install"
      pattern: "dependencies.*gsap"
    - from: "app/layout.tsx"
      to: "next/font"
      via: "import"
      pattern: "next/font/google"
---

<objective>
Initialize Next.js 15 project with GSAP, Tailwind CSS, and TypeScript as the foundation for the luxury landing page.

Purpose: Establish the technical infrastructure recommended in research (Next.js 15 + React 19 + GSAP + Tailwind) with proper configuration for performance and luxury aesthetic development.

Output: Working Next.js project with all dependencies installed and basic configuration in place.
</objective>

<execution_context>
@/Users/forato/.claude/get-shit-done/workflows/execute-plan.md
@/Users/forato/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-core-sections/02-CONTEXT.md
@.planning/phases/02-core-sections/02-RESEARCH.md

# Research References
Research indicates Next.js 15 with Partial Prerendering (PPR) provides optimal LCP performance. GSAP is preferred over Framer Motion for timeline-based luxury animations. Tailwind CSS 4.x provides utility-first styling perfect for minimal luxury aesthetic.
</context>

<tasks>

<task type="auto">
  <name>Initialize Next.js 15 project with TypeScript and Tailwind</name>
  <files>package.json, tsconfig.json, next.config.ts, tailwind.config.ts, postcss.config.mjs</files>
  <action>
Initialize Next.js 15 project with auto-confirmation flags:

```bash
npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir=false --import-alias="@/*" --yes
```

This creates:
- `app/` directory (App Router)
- `public/` directory for static assets
- `package.json` with Next.js 15, React 19, TypeScript, Tailwind CSS
- `tsconfig.json` with strict mode
- `tailwind.config.ts` with content paths
- `next.config.ts` for Next.js configuration

After init, install GSAP:
```bash
npm install gsap
```

Configure `next.config.ts` to enable experimental PPR for performance:
```typescript
const nextConfig = {
  experimental: {
    ppr: 'incremental',
  },
}
```

Do NOT use --use-npm flag with create-next-app as it may cause issues. Let it use the default package manager.
  </action>
  <verify>
Run `npm run dev` and verify server starts on http://localhost:3000 without errors. Check package.json includes "next": "^15", "react": "^19", "gsap": "^3".
  </verify>
  <done>
Dev server starts successfully, all dependencies installed in package.json
  </done>
</task>

<task type="auto">
  <name>Configure root layout with Playfair Display font</name>
  <files>app/layout.tsx, app/globals.css, app/page.tsx</files>
  <action>
Update `app/layout.tsx` to import Playfair Display (Canela fallback per Phase 1 context):

```typescript
import type { Metadata } from "next";
import { Playfair_Display } from "next/font/google";
import "./globals.css";

const playfair = Playfair_Display({
  subsets: ["latin"],
  variable: "--font-playfair",
  display: "swap",
});

export const metadata: Metadata = {
  title: "RE Arquitetura & Design",
  description: "Arquitetura de alto padrão especializada em verticalização",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR" className={`${playfair.variable}`}>
      <body className="antialiased">
        {children}
      </body>
    </html>
  );
}
```

Update `app/globals.css` to extend Tailwind with custom fonts and Quiet Luxury base styles:

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  :root {
    --font-playfair: 'Playfair Display', serif;
  }

  body {
    @apply bg-sand-50 text-gray-900;
    font-family: var(--font-playfair);
  }
}
```

Clear default Next.js content from `app/page.tsx` to prepare for Hero section:

```typescript
export default function Home() {
  return (
    <main>
      {/* Hero section will be added here */}
    </main>
  );
}
```
  </action>
  <verify>
Check that app/layout.tsx has Playfair_Display import and proper metadata. Verify app/globals.css has @tailwind directives and custom CSS variables.
  </verify>
  <done>
Root layout configured with Playfair Display font, base styles applied
  </done>
</task>

<task type="auto">
  <name>Create design tokens from Phase 1 specifications</name>
  <files>app/globals.css, tailwind.config.ts</files>
  <action>
Extend Tailwind config with Phase 1 design tokens (greige, sand, spacing scale):

Update `tailwind.config.ts`:

```typescript
import type { Config } from "tailwindcss";

const config: Config = {
  content: [
    "./pages/**/*.{js,ts,jsx,tsx,mdx}",
    "./components/**/*.{js,ts,jsx,tsx,mdx}",
    "./app/**/*.{js,ts,jsx,tsx,mdx}",
  ],
  theme: {
    extend: {
      colors: {
        greige: {
          50: '#f5f3f0',
          100: '#ebe7e1',
          200: '#ddd5c9',
          300: '#c9bdab',
          400: '#b0a090',
          500: '#9a8b7a', // warm greige with yellow undertones
          600: '#827764',
          700: '#6a6352',
          800: '#555142',
          900: '#454037',
        },
        sand: {
          50: '#faf9f6',
          100: '#f5f3ed',
          200: '#ebe7db',
          300: '#ddd4c0',
          400: '#c9bda8',
          500: '#b5a892', // dominant background per Phase 1 context
          600: '#a0927a',
          700: '#8a7c65',
          800: '#736754',
          900: '#5d5646',
        },
      },
      spacing: {
        // 4px base scale from Phase 1
        '4.5': '18px',
        '5.5': '22px',
        '7': '28px',
        '9': '36px',
        '11': '44px',
        '13': '52px',
        '15': '60px',
        '18': '72px',
        '22': '88px',
        '26': '104px',
      },
      boxShadow: {
        'luxury': '0 1px 3px rgba(0, 0, 0, 0.05)',
        'luxury-lg': '0 4px 12px rgba(0, 0, 0, 0.03)',
      },
      borderRadius: {
        'none': '0px', // sharp corners per Phase 1 context
      },
      fontFamily: {
        serif: ['var(--font-playfair)', 'serif'],
      },
    },
  },
  plugins: [],
};

export default config;
```

Add contrast documentation in globals.css comments for WCAG compliance reference.
  </action>
  <verify>
Run `npx tailwindcss -i app/globals.css -o ./test-output.css --minify` to verify Tailwind builds correctly with custom config. Check that greige and sand colors are defined.
  </verify>
  <done>
Design tokens configured in Tailwind, all Phase 1 color and spacing specifications implemented
  </done>
</task>

</tasks>

<verification>
1. Run `npm run dev` - dev server must start without errors
2. Visit http://localhost:3000 - blank page should load (content added in next plan)
3. Run `npm run build` - production build completes successfully
4. Check package.json contains gsap dependency
5. Verify Playfair Display font loads in browser dev tools
</verification>

<success_criteria>
- Next.js 15 project initialized with TypeScript and Tailwind CSS
- GSAP installed and available for import
- Playfair Display font configured and loading
- Design tokens (greige, sand colors, spacing scale) configured in Tailwind
- Dev server runs without errors on port 3000
- Project is ready for Hero section implementation
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-sections/02-01-SUMMARY.md` with frontmatter documenting tech stack added and key files created.
</output>
