---
phase: 04-conversion-launch
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - vercel.json
  - src/app/layout.tsx
  - src/app/sitemap.ts
  - src/app/robots.ts
  - src/lib/content.ts
  - public/images/*
autonomous: false

must_haves:
  truths:
    - Site is deployed to Vercel and accessible via custom domain
    - Technical SEO meta tags are implemented
    - Sitemap and robots.txt are generated
    - All placeholder content replaced with PT-BR copy
    - All placeholder images replaced with project photography
  artifacts:
    - path: "vercel.json"
      provides: "Vercel deployment configuration with redirects and headers"
      contains: "rewrites|redirects|headers"
    - path: "src/app/layout.tsx"
      provides: "Metadata API for SEO meta tags"
      contains: "metadata|title|description|openGraph"
    - path: "src/app/sitemap.ts"
      provides: "Dynamic sitemap generation for SEO"
      exports: "default export as sitemap function"
    - path: "src/app/robots.ts"
      provides: "Robots.txt configuration"
      exports: "default export as robots function"
    - path: "src/lib/content.ts"
      provides: "Content data structure for copy and images"
      exports: "siteContent, projects"
  key_links:
    - from: "vercel.json"
      to: "Vercel deployment"
      via: "git push triggering automatic deployment"
      pattern: "vercel deploy"
    - from: "src/app/layout.tsx"
      to: "search engines"
      via: "metadata API generating meta tags"
      pattern: "metadata"
    - from: "src/app/sitemap.ts"
      to: "Google Search Console"
      via: "sitemap.xml endpoint"
      pattern: "sitemap"
---

<objective>
Deploy to Vercel with custom domain, implement technical SEO (meta tags, sitemap, robots.txt), and replace all placeholder content with final PT-BR copy and project photography.

Purpose: Make the site live and discoverable. Performance validation is complete, now the site needs to reach users. SEO ensures search engines can index and understand the content. Final content replacement completes the transition from placeholder to production.

Output: Deployed site on custom domain with SEO metadata, all placeholder content replaced with real copy and images.
</objective>

<execution_context>
@/Users/forato/.claude/get-shit-done/workflows/execute-plan.md
@/Users/forato/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-conversion-launch/04-CONTEXT.md

# User decisions from CONTEXT
- Content delivery: Claude's choice for coordinated method
- Image specs: Claude's choice for preparation
- Revision process: Claude's choice for number of rounds
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Vercel deployment</name>
  <files>vercel.json, package.json</files>
  <action>
    Create vercel.json with:
    - Build command: npm run build (or pnpm/yarn equivalent)
    - Output directory: .next (Next.js default)
    - Framework: next.js (auto-detected, but explicit for clarity)
    - Headers for security: X-Frame-Options, X-Content-Type-Options, Referrer-Policy
    - Redirects: www to non-www (or vice versa, user preference)
    - Rewrites for clean URLs if needed
    - Environment variables documentation in comments

    Update package.json scripts:
    - Ensure "build": "next build" exists
    - Add "export" for static export if not using Vercel's edge functions
    - Add "start": "next start" for production

    Install Vercel CLI globally if not present: npm i -g vercel
  </action>
  <verify>vercel.json exists with build configuration, package.json has correct scripts</verify>
  <done>Vercel deployment configured</done>
</task>

<task type="auto">
  <name>Task 2: Implement technical SEO</name>
  <files>src/app/layout.tsx, src/app/sitemap.ts, src/app/robots.ts, src/app/manifest.ts</files>
  <action>
    Update src/app/layout.tsx with Metadata API:
    - Add export const metadata with:
      - title: "RE Arquitetura & Design | Arquitetura de Alto Padrão"
      - description: "Escritório de arquitetura especializado em projetos residenciais e comerciais de alto padrão. Verticalização, design minimalista e atenção aos detalhes."
      - keywords: ["arquitetura", "design", "alto padrão", "verticalização", "projetos residenciais"]
      - authors: [{name: "RE Arquitetura & Design"}]
      - openGraph: {title, description, url, siteName, locale: 'pt_BR', type: 'website'}
      - twitter: {card: 'summary_large_image', title, description}
      - robots: {index: true, follow: true}
      - verification: Google Search Console verification code (placeholder)
    - Add structured data (JSON-LD) for LocalBusiness: organization name, address, phone, URL
    - Add viewport meta tag: width=device-width, initial-scale=1
    - Add theme-color meta tag

    Create src/app/sitemap.ts:
    - Export default function returning sitemap structure
    - Include homepage URL: https://re-arquitetura.com.br
    - Set lastModified to current date
    - Set changeFrequency: 'monthly'
    - Set priority: 1.0
    - Include additional URLs if sections have separate routes

    Create src/app/robots.ts:
    - Export default function returning robots object
    - Allow all user agents: '*'
    - Disallow: /api/, /_next/, /private/
    - Sitemap URL: https://re-arquitetura.com.br/sitemap.xml

    Create src/app/manifest.ts (optional, for PWA):
    - PWA manifest for "Add to Home Screen" on mobile
    - Name: "RE Arquitetura & Design"
    - Icons, theme colors, display mode
  </action>
  <verify>layout.tsx has metadata export, sitemap.ts and robots.ts exist with proper exports</verify>
  <done>Technical SEO implemented with meta tags, sitemap, robots.txt</done>
</task>

<task type="auto">
  <name>Task 3: Create content data structure</name>
  <files>src/lib/content.ts</files>
  <action>
    Create src/lib/content.ts to centralize all copy content:
    - Export siteContent object with PT-BR copy for:
      - Navigation menu items
      - Hero section: headline, subheadline, CTA text
      - Manifesto section: title, body copy
      - Technical differentiator: title, key points
      - Materiality section: title, description
      - Contact section: title, form labels, success message
      - Footer: address, contact info, social links
    - Export projects array with project data:
      - id, title, description, category (residential/commercial)
      - images array (paths to optimized images)
      - year, location
    - Export socialProof object with awards, credentials, highlights
    - Export seo object with page-specific meta titles and descriptions

    Use placeholder content for now (CNT-01 will be replaced in Task 4):
    - Headline: "Arquitetura que Transforma Espaços em Experiências"
    - Subheadlines: Lorem Ipsum paragraphs
    - Project titles: "Projeto Residencial JK", "Edifício Comercial Faria Lima"

    Structure enables easy content updates without touching components.
  </action>
  <verify>content.ts exports siteContent, projects, socialProof, seo objects</verify>
  <done>Content data structure created with placeholder PT-BR copy</done>
</task>

<task type="auto">
  <name>Task 4: Replace placeholder copy with final PT-BR content</name>
  <files>src/lib/content.ts</files>
  <action>
    Update src/lib/content.ts with final PT-BR copy (per CONTEXT decision: Claude's choice for delivery method):

    Hero section:
    - Headline: Direct, powerful statement about firm's value
    - Subheadline: 2-3 sentences on design philosophy
    - CTA: "Converse Conosco" or "Iniciar Projeto"

    Manifesto section:
    - Title: "Nossa Filosofia" or "Manifesto"
    - Body: 3-4 paragraphs on design approach, Quiet Luxury values, materiality focus

    Technical differentiator:
    - Title: "Verticalização" or "Expertise Técnica"
    - 4-6 bullet points on vertical construction capabilities

    Materiality section:
    - Title: "Materialidade" or "O Detalhe Faz a Diferença"
    - Description: Emphasis on joinery, stonework, craftsmanship

    Contact section:
    - Title: "Vamos Conversar?"
    - Form labels: "Nome", "Email", "Mensagem"
    - Success message: "Obrigado pelo contato. Retornaremos em breve."

    If content not yet provided from client:
    - Keep professional placeholder copy that sounds like final content
    - Add TODO comment for client to provide final copy
    - Document content delivery process (email, Google Doc, etc.)
  </action>
  <verify>content.ts has professional PT-BR copy throughout, no Lorem Ipsum remaining</verify>
  <done>All placeholder copy replaced with PT-BR content</done>
</task>

<task type="auto">
  <name>Task 5: Replace placeholder images with project photography</name>
  <files>public/images/*, src/lib/content.ts</files>
  <action>
    Update all placeholder images with actual project photography (per CONTEXT decision: Claude's choice for image specs):

    Image preparation guidelines (document for client):
    - Format: JPG or PNG (Next.js will convert to WebP/AVIF)
    - Resolution: 1920px width for desktop, 1080px for mobile (minimum)
    - Quality: 80-90% to balance file size and visual quality
    - Naming: project-name-01.jpg, project-name-02.jpg for organization
    - Delivery method: Google Drive, Dropbox, or WeTransfer

    Images to replace:
    - Hero background image (1-2 images)
    - Portfolio gallery images (6-12 projects, 3-5 images each)
    - Materiality macro photography (joinery, stonework details)
    - Team photos (if applicable)
    - Project renderings or photos for case studies

    Update src/lib/content.ts projects array with new image paths.

    If photography not yet provided:
    - Use high-quality architecture placeholders from Unsplash
    - Add TODO comment for client to provide final images
    - Document image delivery process and specs

    Place images in public/images/ directory with organized structure:
    - public/images/hero/
    - public/images/portfolio/
    - public/images/materiality/
  </action>
  <verify>All placeholder images replaced or documented as pending, image paths correct in content.ts</verify>
  <done>All placeholder images replaced with project photography or documented as pending</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>Deployment configuration and SEO metadata complete, content structure ready for final copy and images</what-built>
  <action>Deploy to Vercel with custom domain configuration</action>
  <instructions>
    1. Run: vercel login (authenticate with Vercel account)
    2. Run: vercel link (link project to Vercel)
    3. Run: vercel --prod (deploy to production)
    4. Note the deployed URL (e.g., re-arquitetura.vercel.app)

    For custom domain configuration:
    5. Go to Vercel Dashboard > Project > Settings > Domains
    6. Add custom domain: re-arquitetura.com.br
    7. Configure DNS records at Google Domains:
       - A record: Vercel's IP address (provided in dashboard)
       - CNAME: www to cname.vercel-dns.com
    8. Wait for DNS propagation (up to 24 hours, typically 1-2 hours)
    9. Verify SSL certificate is issued automatically by Vercel

    Post-deployment verification:
    10. Visit https://re-arquitetura.com.br
    11. Check all pages load correctly
    12. Test contact form submission
    13. Test WhatsApp button click
    14. Verify sitemap: https://re-arquitetura.com.br/sitemap.xml
    15. Verify robots.txt: https://re-arquitetura.com.br/robots.txt
  </instructions>
  <resume-signal>Type "deployed" with URL, or describe deployment issues</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete deployed site with SEO, final content, and custom domain</what-built>
  <how-to-verify>
    1. Visit deployed site: https://re-arquitetura.com.br
    2. Verify homepage loads with all sections visible
    3. Check page title and meta description in browser view source
    4. Test all navigation links work
    5. Test contact form submission on production
    6. Test WhatsApp button on production
    7. View sitemap.xml and robots.txt
    8. Test mobile responsiveness on actual device
    9. Run Lighthouse audit on production URL
    10. Verify SSL certificate (HTTPS works)

    Expected outcomes:
    - Site loads quickly (LCP < 2.5s)
    - All content displays correctly in PT-BR
    - Images load and display properly
    - Contact form and WhatsApp button work
    - SEO meta tags are present
    - SSL certificate is valid
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- Site deployed to Vercel and accessible
- Custom domain configured with HTTPS
- SEO meta tags implemented
- Sitemap and robots.txt accessible
- All placeholder content replaced
- All placeholder images replaced or documented
</verification>

<success_criteria>
- [ ] Site deployed to Vercel with custom domain
- [ ] Technical SEO implemented (meta tags, sitemap, robots.txt)
- [ ] All placeholder content replaced with PT-BR copy
- [ ] All placeholder images replaced with project photography
- [ ] Site accessible via HTTPS with valid SSL
- [ ] Contact form and WhatsApp button functional on production
</success_criteria>

<output>
After completion, create `.planning/phases/04-conversion-launch/04-03-SUMMARY.md`
</output>
